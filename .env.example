# 复制为 .env 后填写真实值（不要提交到 git）

# -------------------- Runtime --------------------
TZ=Asia/Shanghai
NO_PROXY=127.0.0.1,localhost

# pip 国内镜像源（可选；也可不配，Dockerfile 默认清华源）
PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
PIP_TRUSTED_HOST=pypi.tuna.tsinghua.edu.cn

# APT 国内镜像源（可选；也可不配，Dockerfile 默认清华源）
APT_MIRROR=mirrors.tuna.tsinghua.edu.cn

# apt 国内镜像源（可选；也可不配，Dockerfile 默认清华源）
APT_MIRROR=mirrors.tuna.tsinghua.edu.cn

# 日志目录（默认 ./logs）
LOG_DIR=./logs

# 日报本地保存目录（会在发布时把生成的 md 同步落盘；默认 ./data/news）
REPORT_LOCAL_DIR=./data/news

# 代理（可选）
AZURE_PROXY=http://127.0.0.1:10808

# RSSHub（可选；如果你把 RSSHub 部署在别处就改这里）
RSSHUB_BASE_URL=http://127.0.0.1:1200

# -------------------- Database -------------------
# 数据库后端选择：postgres（默认）或 sqlite
# - postgres：保持你现在的远程 PostgreSQL 用法不变
# - sqlite：本地生成一个 .db 文件（第一次运行自动建表）
DB_BACKEND=postgres

# SQLite 文件路径（DB_BACKEND=sqlite 时生效）
# DB_SQLITE_PATH=./data/news_crawler.db

# 方式1：直接提供 DB_URI（推荐）
# DB_URI=postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require

# 方式2：分别配置（DB_URI 未设置时使用）
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=change-me
DB_NAME=postgres
DB_SSLMODE=require

# -------------------- Mail (SMTP) ----------------
MAIL_HOST=smtp.qq.com
MAIL_PORT=465
MAIL_USER=your@qq.com
MAIL_PASS=your-smtp-auth-code
MAIL_TO=receiver@qq.com
MAIL_FROM_NAME=AI情报员
MAIL_TO_NAME=订阅者

# -------------------- AI (OpenAI compatible) -----
AI_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/
AI_API_KEY=sk-xxxx
AI_MODEL=qwen-flash

# 节流/并发
AI_MAX_WORKERS=1
AI_BASE_DELAY=12
AI_MAX_RETRIES=3

# -------------------- GitHub Publisher -----------
GITHUB_TOKEN=ghp_xxx
REPO_NAME=owner/repo
TARGET_FOLDER=docs/news
